{
  "name": "ci-cd-good-practices",
  "version": "1.0.0",
  "description": "An example project for demonstrating good practices in CI/CD pipelines.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building application...' && mkdir -p dist && touch dist/index.html",
    "test": "echo 'Running tests...' && exit 0",
    "deploy": "echo 'Deploying application...' && ./deploy.sh"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
